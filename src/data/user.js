class User {
    constructor(
      id,
      name,
      email,
      admin_flag
    ) {
      this.id = id;
      this.name = name;
      this.email = email;
      this.admin_flag = admin_flag;
    }
  }
  
  export default User;